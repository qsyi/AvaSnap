# パッチノート

## v0.1.7

- ホームの「最近のプロジェクト」を右クリックして「削除」で、そのプロジェクトをゴミ箱へ移動できるように(完全削除ではないので元に戻せます)
- 90日以上開いていないプロジェクトは、起動時に自動でゴミ箱へ移動するようにしました(ホームの一覧にその旨を表示)
- ホームの「最近のプロジェクト」は新しい順に10件まで表示(一覧の読み込みも高速化)
- プロジェクトのプレビューを、縦長の写真でも切れないように全体表示へ変更
- プロジェクトを開けなかったとき・削除したときに、ホームの一覧の下へ短いメッセージを表示するように
- ホームのレタッチモードに、作業中のプロジェクトがあるときだけ「新規プロジェクトで開く」ボタンを追加(通常の「開く」は「続きから開く」表示に。作業中のものは自動保存済みなので最近のプロジェクトから戻せます)
- 設定パネルの「プロジェクトフォルダを開く」ボタンを削除(ホームの「最近のプロジェクト」欄の「フォルダを開く」に集約)
- VRChatのOSC受信をループバック(127.0.0.1)限定にし、起動時にWindowsファイアウォールの許可を求めないようにしました(同じPCのVRChatからは今まで通り受信します)
- 起動時の古いプロジェクトの整理をバックグラウンド化し、起動のもたつきを軽減
- プレビュー画像の生成頻度を下げて自動保存を軽量化。書き込み中断で残った作業用ファイル(.tmp)を起動時に自動で掃除するように
- 細かい表示の改善(右クリックメニューの見た目をアプリの配色に合わせるなど)

## v0.1.6

- 「写真合成モード」を「レタッチモード」に名称変更(ツールの副題も「VRChatカメラ合成・レタッチツール」に)
- レタッチの作業内容を自動保存する「プロジェクト」機能を追加
  - スライダーなどの値と画像の参照先を1つの`.avasnap`ファイルに保存。編集のたびに自動保存され、アプリを閉じても保持されます
  - 「新規プロジェクト」ボタンで新しいプロジェクトに切り替え。過去のプロジェクトはファイルとして残ります(上書きされません)
  - ホーム画面に過去のプロジェクトがプレビュー付きで並び、クリックで開けます
  - ホームの「最近のプロジェクト」欄の「フォルダを開く」で、プロジェクトの保存先をエクスプローラーで開けます
  - `.avasnap`ファイルをダブルクリックするとAvaSnapで開けるようになりました
  - 起動時は常に新規プロジェクトから始まります
- ウィンドウに画像をドラッグすると、ドロップ先を示すガイドを表示するように
  - 上部左=アバター画像、上部右=背景画像、下部=合成せず1枚だけレタッチ
- アバター画像・背景画像とも、PNG以外の画像形式も読み込めるように
- OSCが検出されないときに通知を出し、VRChat側でOSCを有効化する手引きを表示するように
- 4Kなど拡大表示(高DPI)の環境で、アバター画像の推定配置が大きくずれる不具合を修正
- 「最近のプロジェクト」が一時的なファイルアクセスエラーでまれに丸ごと消える不具合を修正
- 内部整理と細かい表示・操作性の改善

## v0.1.5

- 写真合成モードに「マスクレイヤー」を追加: 効果を画面のどの範囲に効かせるかをブラシで塗って作れます
  - 「マスクを追加」で作成。デカールと同じ枠組みの編集モード(切り抜き後のキャンバスで作業、下部の確定/キャンセル)に入ります
  - 既定は全面が効果あり(白)。ペンで黒く塗ったところが効果の効かない範囲になり、消しゴムで戻せます。ブラシのサイズ・ぼかしを調整できます
  - グラデーション(線形・円形)でもマスクを作れます。ハンドルで引き直せます
  - 「反転」で効く範囲と効かない範囲を入れ替えられます
  - マスクは3個まで。作成・ストローク・グラデーション・割り当ての変更は Ctrl+Z / Ctrl+Y の対象です
  - 作ったマスクは、アバター・背景それぞれの色調補正、トーングラデーション、ライトリークの各スライダーに割り当てて、その範囲だけ効かせられます
- 分割設定の行と保存ボタンの行を入れ替え(分割設定を上に)
- VRChatの写真を検出したときの通知をダークモードに対応
- 内部整理と細かい表示・操作性の改善

## v0.1.4

- 写真合成モードに「デカール」機能を追加: 画像や図形を写真に重ねられます
  - 「画像を追加」でPNG/JPGを重ね、プレビュー上でドラッグ移動・角ハンドルで拡縮・回転ハンドルで回転
  - 「枠線を追加」で写真の縁取り枠を作成: 色・太さを調整でき、角ハンドル=縦横比固定・辺ハンドル=その方向だけ、いずれも中心を基準に拡縮
  - サムネイルのドラッグでレイヤー順を並べ替え。「アバター」マーカーより左に置くとアバターの後ろに合成されます
  - 追加済みのデカールはサムネイルをクリックすると再編集できます
  - 選択中のデカールは矢印キーで移動(Shiftで10px)、Deleteキーで削除
  - デカールごとに不透明度を設定可能
- 背景写真の代わりに「背景なしで作成」を追加: 単色またはグラデーションのキャンバスを作り、デカールだけで背景を組み立てられます(色はリアルタイム反映)
- 背景写真の回転ボタンを追加(押すたびに90°)
- 「アバターなし」と「背景なしで作成」を相互排他に(片方を使うともう片方は無効化)
- すべてのカラーピッカーの明度スライダーを縦向きに変更し、白→黒のグラデーション帯を添えて分かりやすく
- デカールの編集・背景色/グラデーション・写真の回転も Ctrl+Z / Ctrl+Y の対象に
- スポイトで拾った色も Ctrl+Z で戻せるように
- 細かい表示・操作性の改善

## v0.1.3

- 位置合わせモードのFOVガイドをUnity連携に対応: Unity側で対象カメラのFOV・傾きを取得し、AvaSnapのガイド線に反映できるように(要Unity側のjp.qsyi.avasnapパッケージ)
- 取得は「取得」ボタンを押した時だけの単発リクエスト方式。取得できると数秒だけ通知が出ます
- 細かい表示の不具合を修正

## v0.1.2

- 写真合成モードに「切り抜きモード」を追加: プレビュー上で直接ドラッグして切り抜き範囲を調整(角ドラッグでリサイズ、本体ドラッグで移動)。キャンバス比率の「自由」を選ぶと縦横を個別に調整可能
- 写真合成モードに「アバター配置モード」を追加: アバター画像の位置・サイズ・回転をプレビュー上のドラッグ・角ハンドル・回転ハンドルで直接編集(従来のX/Y/幅/回転スライダーは廃止)
- 位置合わせモードもX/Y/幅/高さ/回転スライダーを廃止し、プレビュー上の直接ドラッグ操作に統一(不透明度のみスライダーのまま)
- 切り抜きモード・アバター配置モード中は、確定/キャンセルボタンをプレビュー下部に表示。キャンセルでモード開始時点まで変更を取り消せるように
- 色設定にスポイトツールを追加: プレビュー画像から直接色を拾えるように(ルーペ表示付き)
- キャンバス比率の「カスタム」入力を横:縦の2つの欄に分割
- 細かい表示の不具合を修正(プレビューの余白、比較用スライダーの見切れなど)

## v0.1.1

- タイトルバーを刷新: 標準的なサイズ・配置に変更し、バージョン番号を表示
- ウィンドウの最大化・端をドラッグしてのリサイズに対応
- アップデート通知をタイトルバーのボタンに変更
- 「バージョン情報」と「ライセンス」を別々の画面に分離
- パッチノート機能を追加(この画面です)
- アプリアイコンを追加
- 細かい表示の不具合を修正(コンパクト表示のボタンつぶれ、フォーカス切り替え時の白い枠など)

## v0.1.0

初回リリース。
